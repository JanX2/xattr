{
    IBClasses = (
        {
            ACTIONS = {add = id; open = id; remove = id; }; 
            CLASS = App; 
            LANGUAGE = ObjC; 
            OUTLETS = {ctrl = NSArrayController; table = NSTableView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}